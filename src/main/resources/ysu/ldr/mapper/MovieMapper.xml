<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ysu.ldr.mapper.MovieMapper">
    <select id="selectAllMovie" resultType="Movie">
        select * from movie_info
    </select>
    <select id="countAllMovie" resultType="Integer">
        select count(*) from movie_info
    </select>
    <update id="updateMovieStatusById">
        update movie_info set `show` = #{show} where movie_id = #{movieId}
    </update>
    <select id="getMovieByNameSearch" resultType="Movie">
        select * from movie_info where movie_name like '%${movieName}%'
    </select>
<!--    parameterType 指定了参数类型
        seGeneratedKeys="true" 表示使用数据库自动生成的主键，
        keyProperty="movieId" 则指定了 Java 对象中哪个属性对应于主键。-->
    <insert id="addMovie" parameterType="Movie">
        insert into movie_info (movie_name, release_date, movie_nation, directors,leadactors, editors, picture, avg_rating, num_rating,description, poster, type_id, `show`) values (#{movieName}, #{releaseDate}, #{movieNation}, #{directors}, #{leadactors}, #{editors}, #{picture}, #{avgRating}, #{numRating}, #{description}, #{poster}, #{typeId}, #{show})
    </insert>
<!--    属性太多干脆把Movie类属性给ai，描述一下让它帮我打全-->
    <update id="updateMovie" parameterType="Movie">
        UPDATE movie_info SET
            movie_name = #{movieName},
            release_date = #{releaseDate},
            movie_nation = #{movieNation},
            directors = #{directors},
            leadactors = #{leadactors},
            editors = #{editors},
            picture = #{picture},
            avg_rating = #{avgRating},
            num_rating = #{numRating},
            description = #{description},
            poster = #{poster},
            type_id = #{typeId},
            `show` = #{show}
        WHERE movie_id = #{movieId}
    </update>

    <delete id="deleteMovie" parameterType="Integer">
        delete from movie_info where movie_id = #{movieId}
    </delete>

    <select id="getMovieByTypeSearch" resultType="Movie">
        select * from movie_info
        <where>
            <if test="movieType == 0">
                AND `show` = true
            </if>
            <if test="movieType != 0">
                AND `type_id` = #{movieType} AND `show` = true
            </if>
        </where>

    </select>

    <select id="getMovieById" resultType="Movie">
        select * from movie_info where movie_id = #{movieId}
    </select>

    <select id="getHotMovies" resultType="Movie">
        SELECT *
        FROM movie_info
        ORDER BY num_rating DESC
            LIMIT 10
    </select>

</mapper>